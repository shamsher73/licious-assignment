export interface Book {
    title: string;
    authors: string[];
    cover_id?: number;
    first_publish_year: string;
    description: string;
    cover_i?: number;
    author_name?: string[];
    key: string;
}